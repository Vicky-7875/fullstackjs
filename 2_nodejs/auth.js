// module.exports = {
   function register (user) {
    console.log(`user ${user} register successfully`);
  }
   function login (user) {
    console.log(`user ${user} Login in successfully`);
  }
// };
// module.exports= register;

module.exports = { register, login };
